"""Loss functions specific to the FM model."""
# Currently empty — §3.3 loss hacking uses MultiScaleSpectralLoss from src.ddsp
# with a modified target (e.g. time-reversed audio) rather than a custom loss class.
